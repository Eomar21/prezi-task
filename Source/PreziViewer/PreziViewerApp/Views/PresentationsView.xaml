<UserControl x:Class="PreziViewer.App.Views.PresentationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PreziViewer.App.Views"
             xmlns:viewmodels="clr-namespace:PreziViewer.App.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:PresentationsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <TextBlock Width="300">Presentations View Hola</TextBlock>
        <ScrollViewer>
            <ListBox ItemsSource="{Binding Presentations}"
                     HorizontalContentAlignment="Center"
                     VerticalAlignment="Top"
                     SelectionMode="Single"
                     Width="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:OnePresentationView></local:OnePresentationView>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

    </Grid>
</UserControl>